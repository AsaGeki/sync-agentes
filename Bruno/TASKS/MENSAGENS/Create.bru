meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/projetos/{{slug}}/tasks/{{code}}/mensagens
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  X-Autor-Id: {{autorId}}
}

body:json {
  {
    "type": "pergunta",
    "texto": "Qual o shape de movimentações[] no GET?"
  }
}

docs {
  Endpoint: POST /projetos/:slug/tasks/:code/mensagens
  Autenticação: Bearer token + X-Autor-Id

  type: mudanca | pergunta | resposta | decisao | bloqueio.
  pergunta entra na lista de "perguntas sem resposta" do relatório até um
  evento type=resposta na mesma task.
}
