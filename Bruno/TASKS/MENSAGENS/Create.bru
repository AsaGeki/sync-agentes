meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/projetos/{{slug}}/tasks/{{code}}/mensagens
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  X-Autor-Id: {{autorId}}
}

body:json {
  {
    "type": "pergunta",
    "texto": "Qual o shape de movimentacoes[] no GET?"
  }
}

docs {
  Endpoint: POST /projetos/:slug/tasks/:code/mensagens
  Autenticacao: Bearer token + X-Autor-Id

  type: mudanca | pergunta | resposta | decisao | bloqueio.
  pergunta entra na lista de "perguntas sem resposta" do relatorio ate um
  evento type=resposta na mesma task.
}
