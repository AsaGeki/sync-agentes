meta {
  name: Find All
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/projetos/{{slug}}/tasks
  body: none
  auth: inherit
}

params:query {
  ~status: 
  ~tag: 
}

docs {
  Endpoint: GET /projetos/:slug/tasks
  Autenticação: Bearer token
  
  Query params (opcionais): status, tag.
}
