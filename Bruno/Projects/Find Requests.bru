meta {
  name: Find Requests
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/projetos/{{slug}}/pedidos
  body: none
  auth: inherit
}

docs {
  Endpoint: GET /projetos/{slug}/pedidos
  Autenticacao: token pessoal (owner).
  
  Lista os pedidos de acesso pendentes deste projeto - id, pessoa, email,
  quando pediu.
}
