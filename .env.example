# Host da API (run.py / app/main.py). 127.0.0.1 = so esta maquina (padrao, seguro).
# 0.0.0.0 = exposto na rede - exige regra de firewall e restringir o IP de quem acessa (ver README).
HOST=127.0.0.1

# Porta da API
PORT=8787
